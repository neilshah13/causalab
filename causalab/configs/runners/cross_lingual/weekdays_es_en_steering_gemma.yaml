# @package _global_
# Experiment B (extension) — reverse/cross-source steering, Gemma-3-27B @ L54.
# es ring -> EN prompts. Raises n for the X->EN transfer correlation
# (source isometry vs subspace overlap vs own-coherence). ES is Latin-script,
# full per-language pipeline, so the source ring is well-defined.
defaults:
  - /base
  - /task: natural_domains_arithmetic_weekdays
  - /model: gemma3_27b
  - /analysis/cross_lingual_steering
  - _self_

task:
  target_variable: result

cross_lingual_steering:
  source_task: natural_domains_arithmetic_weekdays_es
  source_experiment_root: artifacts/natural_domains_arithmetic/gemma3_27b/weekdays_es/weekdays_es
  layer: 54
